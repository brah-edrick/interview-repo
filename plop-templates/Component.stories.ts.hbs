import type { Meta, StoryObj } from '@storybook/vue3-vite';

import {{pascalCase name}} from './{{pascalCase name}}.vue';

const meta = {
  title: 'Components/{{pascalCase name}}',
  component: {{pascalCase name}},
  tags: ['autodocs'],
} satisfies Meta<typeof {{pascalCase name}}>;

export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {
  render: () => ({
    components: { {{pascalCase name}} },
    template: '<{{pascalCase name}}>{{pascalCase name}}</{{pascalCase name}}>',
  }),
};

export const CustomText: Story = {
  render: () => ({
    components: { {{pascalCase name}} },
    template: '<{{pascalCase name}}>Custom Text</{{pascalCase name}}>',
  }),
};

export const LongText: Story = {
  render: () => ({
    components: { {{pascalCase name}} },
    template: '<{{pascalCase name}}>This is a longer text example to demonstrate how the component handles extended content</{{pascalCase name}}>',
  }),
};



